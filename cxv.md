下面给你一版**新的总路线图**，按你现在的约束来定：

- **纯 Rust**
- **不保留 FFI**
- **不做自动分类**
- **显式按后缀 / 显式入口调用**
- **优先正确性**
- **模块化推进**
- **可验证性预留**

我会把路线图写成更适合真实落地的版本，而不是“把所有理论一次性铺开”的理想化版本。

---

# dsuni 总路线图 v1

## 0. 总原则

先统一几个长期原则，后面所有阶段都要遵守：

1. **显式求解，不猜类型**
   - 用户必须明确调用 `solve_sat / solve_smt / solve_lp / solve_milp / solve_cp / solve_minlp`
   - 或者通过文件后缀/格式明确指定

2. **统一核心表示**
   - 所有模块共享 `core`
   - 但内部求解 IR 要分层，不让前端 AST 直接渗透到后端

3. **先正确，再优化**
   - 初版以“能稳定解对题”为第一目标
   - 性能优化放后面

4. **每个模块都能独立测试**
   - 每个子系统要有自己的最小可用测试集

5. **先打通闭环，再扩展功能**
   - 每阶段都要有可运行产物，不做空转型设计

---

# 1. 第一阶段：核心地基

## 目标
把整个项目的公共基础打稳，确保后续所有模块都能复用同一套基础设施。

## 要做的内容

### 1.1 core 基础类型
建立统一的基础结构：

- `Var`
- `Sort / Ty`
- `Term`
- `Constraint`
- `Model`
- `Assignment`
- `SolverResult`
- `Config`
- `Status / Reason / Explanation`

### 1.2 统一的错误体系
要区分这些错误：

- 语法错误
- 类型错误
- 约束非法
- 超时
- 内存不足
- 不可判定 / 未支持
- 内部错误

### 1.3 增量求解框架预留
先把接口留出来：

- `push`
- `pop`
- `assume`
- `solve`
- `reset`

即使一开始某些模块还不支持完整增量，也要把结构预留好。

### 1.4 解析层
支持基础输入格式：

- 布尔/算术表达式解析
- 简单文本格式
- 后续再接 SMT-LIB / DIMACS / LP/MPS / MiniZinc 等

### 1.5 规范化层
把用户输入转成内部标准形式：

- 布尔表达式转 NNF/CNF 所需结构
- 算术表达式转线性形式或一般项
- 常量折叠
- 变量收集
- 类型检查

## 本阶段产出
- 一个稳定的 `core`
- 一个基础 parser
- 一个统一的 IR/AST 规范化管线
- 一个能跑的最小 CLI/测试框架

---

# 2. 第二阶段：SAT 求解器

## 目标
先做出一个可靠的 SAT 核心，因为它是后面 SMT 的底座之一。

## 范围
- CNF 输入
- DIMACS 读取
- CDCL 求解
- 基础回溯、传播、学习

## 要做的内容

### 2.1 基础结构
- `Literal`
- `Clause`
- `Trail`
- `Assignment`
- `WatchList`

### 2.2 CDCL 主循环
至少实现：
- unit propagation
- 冲突分析
- 子句学习
- 回溯
- 重新赋值

### 2.3 基础启发式
先用简单版即可：
- variable activity
- clause activity
- 基本重启机制

### 2.4 基础优化
可以后续再加：
- two-watched literals
- clause deletion
- phase saving
- 冲突驱动重启

### 2.5 基础验证
- 小规模 SAT 实例
- 已知可满足 / 不可满足题库
- 随机生成 CNF 回归测试

## 本阶段产出
- `SatSolver`
- DIMACS 解析
- 能稳定解中小规模 CNF
- 提供 `sat` 单独入口

---

# 3. 第三阶段：线性算术核心

## 目标
建立线性约束求解基础，为 LP、LIA、MILP 做公共底座。

## 要做的内容

### 3.1 统一线性表示
建立共享结构：

- `LinearExpr`
- `LinearConstraint`
- `Objective`
- `Bound`
- `Domain`
- `Rational` 或精确有理数表示

### 3.2 规范化
支持：
- 移项
- 合并同类项
- 系数约简
- 不等式统一成标准形
- 等式转双向不等式

### 3.3 数值策略
建议第一版优先：
- 有理数精确推理
- 或至少保证理论层使用精确表示
- 浮点只作为性能后备，不作为逻辑核心

### 3.4 线性可行性检查
先做最基础版本：
- 约束传播
- 简单可行性检查
- 后续接单纯形

## 本阶段产出
- 公共 `linear` 子模块
- 统一的线性表达接口
- 可被 SMT / MILP / CP 复用

---

# 4. 第四阶段：LP 求解器

## 目标
做出一个纯 Rust 的基础 LP 求解器，作为 MILP 和 SMT-LRA 的核心支撑。

## 范围
- 线性规划可行性
- 可选目标函数优化
- 精确优先

## 要做的内容

### 4.1 单纯形基础实现
建议先做：
- 修正单纯形
- tableau 或 dictionary 形式
- 可行解维护
- 退化处理基础逻辑

### 4.2 预处理
- 冗余约束去除
- 常量传播
- bound tightening
- 简单 infeasible 检测

### 4.3 基础数值鲁棒性
- 精确算术优先
- 设计好 pivot / ratio test 的边界处理
- 避免“看似可行、实际错误”的结果

### 4.4 结果类型
- feasible
- infeasible
- unbounded
- unknown

## 本阶段产出
- `LpSolver`
- 一套能独立运行的 LP 测试集
- 能为 SMT/MILP 提供底层支撑

---

# 5. 第五阶段：SMT 基础框架

## 目标
构建 SMT 的理论组合框架，但先只做少数可控理论，不追求一口气全覆盖。

## 首批支持理论
建议优先顺序：

1. `QF_UF`
2. `QF_LRA`
3. `QF_LIA`
4. `UF + LRA`
5. `UF + LIA`

## 要做的内容

### 5.1 SAT + theory 框架
- 布尔骨架交给 SAT
- 理论约束交给 theory solver
- conflict / propagation 接口打通

### 5.2 理论求解器接口
定义统一 trait，例如：

- `assert_literal`
- `propagate`
- `check`
- `explain_conflict`
- `push/pop`

### 5.3 E-graph / congruence closure
用于 UF：
- 等价类维护
- 合并传播
- 一致性检测

### 5.4 LRA / LIA 接口
- LRA 直接复用 LP
- LIA 先做基础整数一致性
- 后续再扩展更强整数推理

### 5.5 理论冲突解释
这是 SMT 非常重要的一环：
- 冲突不能只返回“错了”
- 要能回到 SAT 层形成学习子句

## 本阶段产出
- `SmtSolver`
- 最小组合理论框架
- 可以跑 `UF`、`LRA`、`LIA` 子集题目

---

# 6. 第六阶段：MILP

## 目标
在 LP 和线性表示稳定后，再上 MILP。

## 要做的内容

### 6.1 分支定界
- 变量分支
- 节点管理
- 下界/上界维护
- incumbent 更新

### 6.2 LP relaxation
- 每个节点调用 LP
- 以 LP 结果指导剪枝

### 6.3 割平面
初版可先做基础几类：
- Gomory cut
- bound cuts
- 简单 cover cuts

### 6.4 启发式
- 变量选择
- 节点选择
- 取整启发式

### 6.5 可行性处理
- 整数性检查
- 松弛解修复
- 剪枝策略

## 本阶段产出
- `MilpSolver`
- 能解决基础整数线性问题
- 与 LP 共享底层线性结构

---

# 7. 第七阶段：CP

## 目标
建立约束规划求解能力，强调传播和回溯。

## 要做的内容

### 7.1 变量域
- 离散域表示
- 区间域
- 集合域预留

### 7.2 传播框架
- 事件驱动传播
- 固定点迭代
- 约束活跃队列

### 7.3 基础全局约束
优先做：
- `all_different`
- `linear`
- `element`
- `sum`
- `cumulative` 可后置

### 7.4 搜索
- 回溯
- 变量顺序启发式
- 值顺序启发式
- 基础失败回跳可后续再加

## 本阶段产出
- `CpSolver`
- 传播 + 回溯框架
- 可解一批经典 CP 小题

---

# 8. 第八阶段：MINLP

## 目标
在前面基础足够稳之后，再做 MINLP，不要过早上。

## 要做的内容

### 8.1 外逼近框架
- 线性松弛
- 切平面近似
- 节点分支

### 8.2 非线性处理
- 乘法项分解
- 区间界定
- 分段线性近似
- 可选的局部优化器内核

### 8.3 全局搜索框架
- branch and bound
- pruning by bounds
- incumbent 管理

### 8.4 适用范围收紧
初版只支持：
- 一部分多项式约束
- 低阶非线性
- 不追求全局最强能力

## 本阶段产出
- `MinlpSolver`
- 可以解决一部分中小规模非线性整数问题

---

# 9. 第九阶段：统一入口与显式调度

## 目标
让整个系统对外使用时清晰、稳定、无歧义。

## 建议的调用方式

### API 层
- `solve_sat(...)`
- `solve_smt(...)`
- `solve_lp(...)`
- `solve_milp(...)`
- `solve_cp(...)`
- `solve_minlp(...)`

### CLI 层
- `dsuni sat file.cnf`
- `dsuni smt file.smt2`
- `dsuni lp file.lp`
- `dsuni milp file.mps`
- `dsuni cp file.mzn`
- `dsuni minlp file.xxx`

## 这一阶段做什么
- 统一参数配置
- 统一结果格式
- 统一日志系统
- 统一统计信息
- 统一超时/中断处理

## 本阶段产出
- 完整对外接口
- 用户不需要理解内部模块结构也能使用

---

# 10. 第十阶段：验证、测试、回归体系

## 目标
把项目从“能跑”推进到“可信”。

## 要做的内容

### 10.1 单元测试
每个模块都要有：
- 最小样例
- 边界样例
- 退化样例
- 随机样例

### 10.2 回归测试
建议分题库类型：
- SAT：DIMACS
- SMT：SMT-LIB
- LP/MILP：MIPLIB / 自造小例子
- CP：MiniZinc 风格题
- MINLP：精选非线性小题

### 10.3 属性测试
适合验证：
- 规范化前后等价性
- 传播幂等性
- 回溯一致性
- 求解结果与模型一致性

### 10.4 形式化验证预留
先只验证局部关键组件：
- watched literal 不变式
- domain pruning 正确性
- 线性规范化等价性
- trail / assignment 一致性

## 本阶段产出
- 稳定 CI
- 回归集
- 可持续迭代的正确性保障体系

---

# 11. 推荐的实际开发顺序

如果你问我“最现实的先后顺序”，我会建议：

### 先做这几个
1. `core`
2. `parser`
3. `sat`
4. `linear`
5. `lp`

### 然后
6. `smt`
7. `milp`
8. `cp`

### 最后
9. `minlp`

因为：
- SAT 最容易建立闭环
- linear + LP 是很多模块共同底座
- SMT 和 MILP 都可以复用这些基础
- MINLP 最难，必须放后

---

# 12. 版本里程碑建议

## v0.1
- core
- parser
- SAT
- 基础 CLI

## v0.2
- linear
- LP
- 基础测试体系

## v0.3
- SMT 基础框架
- LRA / UF / LIA 初版

## v0.4
- MILP 初版
- branch and bound

## v0.5
- CP 初版

## v0.6
- MINLP 初版
- 全部统一入口完善

## v1.0
- 核心模块稳定
- 回归测试完整
- 文档完善
- 可对外公开使用

---

# 13. 一句话总结这条新路线

**先把公共地基和 SAT/LP 跑通，再做 SMT 和 MILP，CP 作为并行分支补齐，MINLP 放最后；所有入口显式调用，不做自动识别，不接 FFI。**
